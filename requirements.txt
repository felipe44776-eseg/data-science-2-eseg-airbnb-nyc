# Versoes fixadas: as que produziram os numeros publicados (Python 3.11).

# --- ingestao ---
requests==2.34.2
pandas==3.0.6
pyarrow==25.0.1           # parquet
numpy==2.4.6

# --- geoespacial ---
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0           # leitor/escritor vetorial do geopandas
h3==4.5.0                 # indexacao hexagonal — unidade espacial do projeto (ADR 0001)

# --- analise / modelagem ---
scipy==1.17.1
scikit-learn==1.9.1
lightgbm==4.7.0           # preco, ocupacao, sobrevivencia; exportado para o site
statsmodels==0.15.0
shap==0.51.0
libpysal==4.14.1          # pesos espaciais
esda==2.9.0               # Moran global e local (LISA)

# --- visualizacao ---
matplotlib==3.11.2

# --- documentacao ---
mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==12.0.1

# --- notebooks (so executam; a logica mora em src/) ---
ipykernel==7.3.0
nbformat==5.11.1
nbclient==0.11.0

# --- dev ---
pytest==9.1.1
ruff==0.16.8
